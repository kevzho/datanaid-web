# Datanaid Python ML microservice (Vercel Python runtime).
# This file exists so Vercel can detect the OPTIONAL /api/forecast Python function.
# The app works fully without this function (TS linear forecast is the default).
#
# NOTE: Keep this file intentionally empty for Vercel Hobby/free deployments.
# Prophet, pandas, numpy, xgboost, and scikit-learn exceed Vercel's Python
# serverless storage limit when bundled together. The /api/forecast endpoint
# gracefully falls back to its built-in pure-Python linear forecast.
#
# For a separate paid/heavier Python service, install these outside this Vercel
# app and point PYTHON_ML_URL at that service instead:
# prophet==1.1.5
# pandas==2.2.2
# numpy==1.26.4
# xgboost==2.0.3
# scikit-learn==1.4.2
